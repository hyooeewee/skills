# # 杂项

我保留但很少使用的工具——插件中未推广。

* 设置 Claude Code 钩子，在危险 git 命令（如 push、reset --hard、clean 等）执行之前将其拦截。
* 将测试文件从 `as` 类型断言迁移到 @total-typescript/shoehorn。
* 创建包含章节、问题、解决方案和解释说明的练习目录结构。
* 使用 lint-staged、Prettier、类型检查和测试设置 Husky pre-commit 钩子。
