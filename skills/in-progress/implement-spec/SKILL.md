---
name: implement-spec
description: 在代码中实现规范。
disable-model-invocation: true

---

你收到了一个规范。该规范应该有关联的任务，用来描述如何实现该规范。

目标是创建一个 PR，在单个分支上实现整个规范。

任务不是步骤列表。它们是一个具有阻塞关系的**任务图**。这意味着总有一组（**前沿**）任务准备好被获取。

与子代理之间的通信应保持简练。主要通过**上下文指针**进行沟通：指向规范、任务、研究笔记和以前的提交。不要复制通过指针已经获取的信息。

**实现者子代理**应在可能的情况下在后台运行，以实现**最大并发**。

## 步骤

1. 读取规范和任务。阅读足够的内容以理解任务图。

2. （可选）使用**探索子代理**执行任务所需的任何探索——相关的代码库文件或外部文档。确保探索子代理可以保存文件——它应该将其 markdown 笔记保存在仓库外的目录中，供所有未来的子代理访问。这样可以让**实现者子代理**专注于实现，而不是探索。

3. 创建一个分支和一个草稿 PR。PR 应标记为“关闭”规范问题（issue）和任务。

4. 使用**实现者子代理**来实现每个任务。每个实现者子代理应在自己的工作树和分支上工作。

5. 一旦**实现者子代理**完成，使用**合并者子代理**将其工作合并到 PR 分支。

6. 如果这改变了可用任务的**前沿**，则启动更多**实现者子代理**来处理新任务。这允许实现最大并发。

7. 一旦所有任务完成，在 PR 分支上运行 /code-review。在一个**实现者子代理**中修复代码审查提出的所有问题。

8. 将 PR 标记为准备好审查。

9. 清理所有**实现者子代理**的工作树。
