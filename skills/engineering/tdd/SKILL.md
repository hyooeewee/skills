---
name: tdd
description: 测试驱动开发。当用户想要以测试优先的方式构建功能或修复错误、提及"red-green-refactor"，或想要集成测试时使用。

---

# 测试驱动开发

TDD 是红 → 绿循环。这项技能是让该循环产生值得保留的测试的参考：什么是好的测试、测试放在哪里、反模式以及循环的规则。每一节都适用于每个周期：在循环期间和之前咨询它们，而不是之后。

探索代码库时，请阅读 `CONTEXT.md`（如果存在），以便测试名称和接口词汇与项目的领域语言保持一致，并尊重你所接触区域的 ADR。

## 什么是好的测试

测试通过公共接口验证行为，而不是实现细节。代码可以完全改变；测试不应改变。一个好的测试读起来像规格说明："用户可以使用有效的购物车结账" 告诉你确切存在什么能力，并且它能够经受重构，因为它不关心内部结构。

有关示例，请参阅 [tests.md](tests.md)；有关模拟指南，请参阅 [mocking.md](mocking.md)。

## Seams: where tests go

**接缝**是你进行测试的公共边界：一个无需深入内部即可观察行为的接口。测试存在于接缝处，绝不对着内部实现。

**仅在预先商定的接缝处进行测试。** 在编写任何测试之前，写下正在测试的接缝并与用户确认。在未确认的接缝上不会编写任何测试。你无法测试所有内容，因此预先商定接缝是让测试工作落在关键路径和复杂逻辑上，而不是每个边缘情况上的方法。

Ask: "What's the public interface, and which seams should we test?"

当该接口的形状本身存疑时（模块有多深、接缝属于哪里、接口应该暴露什么），调用 Skill 工具并使用 "codebase-design" 来获取词汇。它是模块、接口、深度、接缝、适配器、杠杆作用和局部性等术语的共享来源，它是供参考的资料，而不是要运行的会话。

## 反模式

* **实现耦合**：模拟内部协作者、测试私有方法，或通过旁路通道验证（查询数据库而不是使用接口）。线索是：当你重构时测试会失败，但行为没有改变。
* **同义反复**：断言以代码执行的方式重新计算期望值（`expect(add(a, b)).toBe(a + b)`，同样方式手动派生的快照，断言等于自身的常量），因此它天生通过，并且永远不会与代码相悖。期望值必须来自独立的事实来源：已知良好的字面量、经过推演的示例或规格说明。
* **水平切片**：先编写所有测试，然后再编写所有实现。批量测试验证的是*想象*的行为：你测试的是事物的*形状*而不是面向用户的行为，测试对真实变化变得不敏感，且你在理解实现之前就承诺了测试结构。相反，在**垂直切片**中工作：一个测试 → 一个实现 → 重复，每个测试都是一颗**追踪子弹**，响应上一个周期教给你的内容。

## 循环的规则

* **先红后绿。** 先编写失败的测试，然后只编写足以通过它的代码。不要预想未来的测试，也不要添加推测性的功能。
* **一次一个切片。** 每个周期一个接缝、一个测试、一个最小实现。
* **重构不属于循环的一部分。** 它属于审查阶段（参见 `code-review` 技能），而不是红 → 绿的实现循环。
